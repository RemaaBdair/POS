import React from "react";
import { MyButton } from "../components/Button";

export default {
  title: "Button",
};

export const LogInButton = () => (
  <MyButton bgColor="#1861ab" color="#ffffff">
    Login
  </MyButton>
);
