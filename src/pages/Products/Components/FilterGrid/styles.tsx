import { createStyles } from "@material-ui/core/styles";
export const styles = createStyles({
  datePicker: { marginLeft: 16, marginTop: 0 },
  submitButton: {
    margin: "0px 0px 0px 16px",
    padding: "8px 16px",
  },
});
